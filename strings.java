import java.util.*;
public class strings {
    
//Strings are immutatable means once it is declared we cannot change it.
public static void main(String[] args) {
    Scanner sc=new Scanner(System.in);
    String name;
    name=sc.nextLine();
    System.out.println(name);
}
}